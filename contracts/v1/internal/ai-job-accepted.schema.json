{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:agentic-itsm:contracts:v1:internal:ai-job-accepted",
  "title": "AI Job Accepted",
  "description": "Internal FastAPI acknowledgement that one durable execution attempt was accepted for processing.",
  "type": "object",
  "required": [
    "job_id",
    "attempt",
    "status",
    "duplicate"
  ],
  "properties": {
    "job_id": {
      "type": "string",
      "minLength": 1,
      "description": "Phoenix-owned durable job identifier."
    },
    "attempt": {
      "type": "integer",
      "minimum": 1,
      "description": "Durable execution attempt acknowledged by the AI service."
    },
    "status": {
      "const": "accepted",
      "description": "Stable acknowledgement state for accepted AI execution."
    },
    "duplicate": {
      "type": "boolean",
      "description": "True when this execution request matched an already-active job attempt."
    }
  },
  "additionalProperties": false
}